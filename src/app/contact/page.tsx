import { Contact } from "@/components/sections/Contact"

export default function ContactPage() {
  return (
    <main className="min-h-screen bg-background text-slate-50 selection:bg-primary/30 pt-20 noise-panel">
      <Contact />
    </main>
  )
}
